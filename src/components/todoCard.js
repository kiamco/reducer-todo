import React from 'react';

const TodoCart = (props) => {
    return ( <
        div className = 'list-item' >
        <
        h1 > { props.item } < /h1> <
        /div>
    )
}

export default TodoCart;